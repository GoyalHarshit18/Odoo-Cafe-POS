# Server Config
PORT=5000
NODE_ENV=production
JWT_SECRET=your_super_secret_production_key

# Database Config (Pick one)
# 1. Using Connection String (Recommended for Supabase/Render)
DATABASE_URL=postgres://user:password@host:port/dbname

# 2. Manual Config (If connection string is not available)
DB_NAME=odoo_cafe_pos
DB_USER=postgres
DB_PASS=your_password
DB_HOST=localhost
DB_PORT=5432
